const { Thought, User, Reaction } = require("../models");

const thoughtController = {

    // GET all thoughts.

    // GET thought by ID.

    // Add thought.

    // Update thought.

    // Delete thought.

};


module.exports = thoughtController;